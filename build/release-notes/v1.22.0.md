Drag your device cards into any order (Pro)

- **New (Pro): drag-to-reorder cards.** Every device card on the home dashboard now has a grip handle next to the bulk-tuning checkbox — drag it to arrange your fleet however makes sense to you: favorite miner up top, a problem device right where you'll spot it. Reordering is scoped to one coin section at a time, since a device's section comes from which pool it's mining to, not from where its card sits.
- Your custom order shows up on the iOS/Android app automatically — nothing to set up there, the phone app just renders whatever order the desktop sends it.
- Free tier sees the same grip handle, locked, with a note on what upgrading unlocks.
